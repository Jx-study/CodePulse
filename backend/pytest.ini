[pytest]
addopts = -m "not integration"
markers =
    integration: marks tests that require external services (e.g. Docker). Run with -m integration to include.
    smoke: marks slow end-to-end tests that load the real MiniLM model. Run with -m smoke to include.
